library("tdr-jenkinslib")

deployToLambda(
        stage: params.STAGE,
        libraryName: "download-files",
        version: params.TO_DEPLOY,
        deploymentPackageName: "download-files.jar"
)
